<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.logan.cuponix">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".IniciarSesionActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegistroActivity" />
        <activity
            android:name=".PrincipalActivity"
            android:label="@string/title_activity_principal"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".CategoriasActivity" />
        <activity android:name=".EmpresasActivity" />
        <activity android:name=".SucursalesActivity" />
        <activity android:name=".PromocionesActivity" />
        <activity android:name=".CuponActivity" />
        <activity android:name=".MisCuponesActivity" />
        <activity android:name=".EditarPerfilActivity"></activity>
    </application>

</manifest>